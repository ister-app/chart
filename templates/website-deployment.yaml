apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ister.fullname" . }}-website
  labels:
    {{- include "ister.labels" (dict "ctx" . "component" "website") | nindent 4 }}
spec:
  replicas: {{ .Values.website.replicaCount }}
  selector:
    matchLabels:
      {{- include "ister.selectorLabels" (dict "ctx" . "component" "website") | nindent 6 }}
  strategy:
    # Stateless: roll it without downtime.
    type: RollingUpdate
  template:
    metadata:
      labels:
        {{- include "ister.selectorLabels" (dict "ctx" . "component" "website") | nindent 8 }}
      annotations:
        {{- if .Values.website.wellKnown.enabled }}
        # Roll the pod when the discovery document changes (server.name, OIDC, URL).
        checksum/wellknown: {{ include "ister.wellKnownDocument" . | sha256sum }}
        {{- end }}
        {{- with .Values.website.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "ister.serviceAccountName" . }}
      automountServiceAccountToken: false
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.website.podSecurityContext | nindent 8 }}
      {{- with .Values.website.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.website.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.website.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: website
          image: {{ include "ister.image" (dict "ctx" . "image" .Values.website.image) | quote }}
          imagePullPolicy: {{ .Values.website.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.website.securityContext | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.website.port }}
          {{- with .Values.website.extraEnv }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if .Values.website.wellKnown.enabled }}
          volumeMounts:
            - name: wellknown
              mountPath: /usr/share/nginx/html/.well-known/ister
              subPath: ister
              readOnly: true
          {{- end }}
          {{- if .Values.website.probes.enabled }}
          {{- $probe := list "curl" "-fsS" "-o" "/dev/null" (printf "http://127.0.0.1:%d%s" (int .Values.website.port) .Values.website.probes.path) }}
          # exec and not httpGet, for the same reason the database and broker probes are
          # exec: kubelet aims an httpGet at the pod's FIRST IP, which on an
          # IPv6-primary cluster is the IPv6 one, and the player image's nginx listens
          # on IPv4 only. That restarted a healthy pod in a loop while its Service
          # answered fine. 127.0.0.1 inside the container is right either way.
          readinessProbe:
            exec:
              command: {{ toJson $probe }}
            periodSeconds: 10
          livenessProbe:
            exec:
              command: {{ toJson $probe }}
            periodSeconds: 20
          {{- end }}
          resources:
            {{- toYaml .Values.website.resources | nindent 12 }}
      {{- if .Values.website.wellKnown.enabled }}
      volumes:
        - name: wellknown
          configMap:
            name: {{ include "ister.fullname" . }}-wellknown
      {{- end }}
